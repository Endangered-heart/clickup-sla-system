name: ClickUp SLA Alerts

on:
  schedule:
    - cron: '30 3 * * *'
    - cron: '30 11 * * *'
  workflow_dispatch:

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - name: Send SLA Report to Slack
        run: curl -s -X POST https://clickup-sla-system-production.up.railway.app/report
