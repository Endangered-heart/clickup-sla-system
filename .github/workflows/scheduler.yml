name: Run ClickUp SLA System

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger ClickUp SLA System
        run: curl https://clickup-sla-system-production.up.railway.app
