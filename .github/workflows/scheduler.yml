name: ClickUp Sync (Silent)

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Sync ClickUp tasks (no Slack)
        run: curl -s -X POST https://clickup-sla-system-production.up.railway.app/sync
